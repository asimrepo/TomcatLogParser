
To run it there are multiple ways. I will list two of them
1. 
a. Install Python
b. my python py.exe is callable from C:\Windows
c. opened cmd and change the directory to C:\Windows
d. I created the script in C:\Windows
e. on cmd run py.exe  C:\Windows\ScriptLogParserWithoutInputParameter.py
f. The script will be executed and it will show all THE UNIQUE URL and Total time each it visited. Also it will show average execution time of each URL.It different from last script which name was ScriptLogParser.py,which asked the user for input to enter but this time you only need to execute without any input parameter and it will show all UNIQUE URL, total invocations and Average execution time.
2. Install PyCharm and open the script and execute